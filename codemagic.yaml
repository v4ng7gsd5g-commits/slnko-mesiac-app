workflows:
  default-workflow:
    name: Default Workflow
    instance_type: mac_mini_m2
    scripts:
      - name: Build for iOS Device
        script: | 
          flutter pub get
          flutter build ios --release --no-codesign

    # TENTO RIADOK DOPLŇ SEM (Zarovnaj ho presne pod 'scripts'):
    artifacts:
      - build/ios/iphoneos/Runner.app
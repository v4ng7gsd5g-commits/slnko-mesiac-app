name: moja_prva_appka
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.11.0 # Základná kamera (oveľa stabilnejšia)
  geolocator: ^13.0.1
  apsl_sun_calc: ^0.0.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Ak budeš neskôr pridávať vlastné obrázky (mesiaca/slnka), 
  # odkomentuješ sekciu assets nižšie:
  # assets:
  #   - assets/images/